// Global test setup for QueryGenAI backend
// Load environment variables (including .env.test) before any test imports
import '../src/config/env.js';

// No database push here; handled by globalSetup to run once per test suite.
